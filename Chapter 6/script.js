//arrays in javascript

//in arrays we put in a list and we access them using numeric indexes
// we can also add boolean and null
let names = ["chitresh","siddesh","raj","varun",6,true,null];

console.log(names[3]);
console.log(names[1]);
console.log(names[2]);
console.log(names[5]);

console.log(typeof(names));//this will give the output objects

//arrays are objects
//the only reference type is actually object